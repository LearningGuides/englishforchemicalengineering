---
interface Props {
  id: string;
  title: string;
  lessonNumber?: number;
}

const {
  id,
  title,
  lessonNumber,
} = Astro.props;
---

<article class="lesson-card">
  <h2>
    <a href={`${import.meta.env.BASE_URL}lessons/${id}`}>
      {title}
    </a>
  </h2>
</article>